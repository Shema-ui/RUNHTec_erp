# Full URL of the backend API, no trailing slash. This project's backend is
# a Node/Express/MySQL server (apps/server) — it replaced an earlier
# PocketBase backend because PocketBase cannot run on Hostinger shared
# hosting. Required for any real deployment.
VITE_API_URL=https://api.runhteccontractors.com
